# 🚀 Быстрый старт Dashboard

## 1. Запуск Dashboard

```bash
# Клонируй репо на VPS
cd /var/www
git clone https://github.com/ainerovich/avito-parser.git
cd avito-parser

# Запусти Dashboard
bash start-dashboard.sh
```

**Dashboard откроется на:** http://YOUR_VPS_IP:5000

---

## 2. Настройка через веб-интерфейс

### Шаг 1: Город
1. Введи город на латинице (как в URL Авито): `vorkuta`
2. Жми "Сгенерировать ссылки"
3. Система автоматически создаст ссылки для всех категорий

### Шаг 2: Ссылки
1. Включи/отключи нужные категории переключателями
2. Жми "Сохранить ссылки"

### Шаг 3: VK токен
1. Получи токен: https://vkhost.github.io/
   - Выбери "Standalone приложение"
   - Права: `wall, photos, groups, offline`
2. Вставь токен в поле "VK Access Token"
3. Введи ID групп (с минусом!):
   - Открой группу в VK
   - URL: `vk.com/club123456789`
   - ID: `-123456789`
4. Жми "Сохранить VK"

### Шаг 4: Telegram (опционально)
1. Создай бота через @BotFather
2. Получи токен
3. Добавь бота в админы каналов
4. Введи токен и ID каналов (@username или числовой ID)
5. Жми "Сохранить Telegram"

### Шаг 5: Прокси (опционально)
1. Вставь прокси (по одному на строку)
2. Форматы:
   ```
   http://ip:port
   http://user:pass@ip:port
   socks5://ip:port
   ```
3. Жми "Сохранить прокси"

### Шаг 6: Стоп-слова
1. Добавь слова для фильтрации рекламы
2. По одному на строку
3. Жми "Сохранить стоп-слова"

---

## 3. Запуск парсера

```bash
# В новом терминале
cd /var/www/avito-parser
bash start-parser.sh
```

Парсер будет работать в фоне и публиковать объявления автоматически!

---

## 4. Просмотр статистики

Dashboard показывает:
- 📊 Всего объявлений
- 🆕 Новых
- ✅ Опубликованных

Обновляется каждые 10 секунд.

---

## 5. Доступ снаружи

Если хочешь открыть Dashboard извне:

```bash
# На VPS
ufw allow 5000/tcp

# Или через nginx (рекомендуется)
```

Создай `/etc/nginx/sites-available/avito-parser`:

```nginx
server {
    listen 80;
    server_name parser.yourdomain.com;

    location / {
        proxy_pass http://localhost:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

Активируй:
```bash
ln -s /etc/nginx/sites-available/avito-parser /etc/nginx/sites-enabled/
systemctl reload nginx
```

---

## Готово! 🎉

Теперь у тебя есть:
- ✅ Веб-интерфейс для настройки
- ✅ Автоматический парсинг
- ✅ Публикация в VK и Telegram
- ✅ Фильтрация рекламы
- ✅ Статистика

**Следующие шаги:**
- Добавим поддержку нескольких городов
- Webhook для моментальных обновлений
- Расширенную аналитику
