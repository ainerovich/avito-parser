# 🎉 Авито Парсер v2 - Полная переработка

## Что изменилось

### ✅ Новые возможности

**Города:**
- Добавляй города на русском языке
- Каждый город может быть включен/отключен переключателем
- Разные пулы ссылок для каждого города
- Визуальные карточки с модулями ссылок

**Глобальная конфигурация:**
- Один VK токен для всех групп
- Один Telegram токен для всех каналов
- Общий пул прокси для всех городов
- Общие стоп-слова для всех городов

**Улучшенный парсер:**
- Ротация прокси между запросами
- Случайные задержки (антибан механизм)
- Лучшее извлечение цены и данных
- Определение типа автора (частное лицо / бизнес)
- Фильтрация по длине описания (защита от спама)
- Обработка ошибок без остановки

**Dashboard v2:**
- Карточки городов с визуализацией ссылок
- Переключатели для каждой ссылки
- Добавление новых городов по-русски
- Единая точка управления всеми токенами и прокси

---

## Как использовать v2

### 1. Замень Dashboard

```bash
# На VPS
cd /var/www/avito-parser/frontend

# Заменяй старый на новый
mv index.html index_v1.html
cp index_cities.html index.html
```

### 2. Обнови бэкенд

```bash
cd /var/www/avito-parser/backend

# Обнови config.yaml
cp config_new.yaml config.yaml

# Обнови API (замени старый на новый)
mv api.py api_v1.py
cp api_improved.py api.py

# Обнови парсер (может работать параллельно со старым)
cp parser_improved.py parser_v2.py
```

### 3. Перезагрузи Dashboard

```bash
# Жми Ctrl+C в терминале где запущен Dashboard
# Запусти снова
bash start-dashboard.sh
```

### 4. Откройся в браузере

http://151.247.209.203:5000

---

## Структура v2 config.yaml

```yaml
# Один токен для всех групп
vk:
  access_token: "vk1.a..."
  groups:
    auto: "-123456789"
    real_estate_sale: "-987654321"
    # ... остальные категории

# Один токен для всех каналов
telegram:
  bot_token: "123:ABC..."
  channels:
    auto: "@avto"
    real_estate_sale: "@realty"

# Общие для всех городов
proxies: ["http://ip:port", ...]
stop_words: ["рассрочка", "кредит", ...]

# ГОРОДА - каждый со своим пулом ссылок
cities:
  - name: "Воркута"           # На русском!
    url_slug: "vorkuta"       # На латинице
    enabled: true             # Переключатель
    sources:                  # Пул ссылок для этого города
      - category: "auto"
        url_path: "avtomobili"
        enabled: true
        signature: "🚗 Авто Воркута"
      - category: "real_estate_sale"
        url_path: "kvartiry"
        enabled: true
        signature: "🏠 Недвижимость Воркута"
      
  - name: "Москва"
    url_slug: "moskva"
    enabled: false
    sources: [...]
```

---

## API v2 - новые endpoints

```
POST /api/cities                  - Добавить город
GET  /api/cities                  - Получить города
POST /api/cities/<slug>/toggle    - Вкл/выкл город
POST /api/cities/<slug>/sources   - Обновить ссылки города
DELETE /api/cities/<slug>         - Удалить город

POST /api/tokens/vk               - Обновить VK токен
POST /api/tokens/telegram         - Обновить TG токен

POST /api/proxies                 - Обновить прокси
POST /api/stop-words              - Обновить стоп-слова
```

---

## Dashboard v2 - что можешь делать

1. **Добавить город:**
   - Введи название на русском (Воркута)
   - Введи слаг на латинице (vorkuta)
   - Система автоматически создаст ссылки

2. **Управлять ссылками города:**
   - Переключи нужные категории
   - Жми "💾" чтобы сохранить

3. **Включить/отключить город:**
   - Переключатель рядом с названием города

4. **Удалить город:**
   - Красная кнопка 🗑️

5. **Настроить токены:**
   - Один VK токен для всех
   - Один TG токен для всех

6. **Добавить прокси:**
   - Общий пул для всех городов
   - По одному на строку

7. **Наполнить все города:**
   - Кнопки: День / 3 дня / 5 дней
   - Парсит все активные города сразу

---

## Миграция с v1 на v2

### Если хочешь оставить v1:
```bash
# Просто добавь v2 как отдельный API
# Оба могут работать параллельно на разных портах
```

### Если хочешь полностью перейти на v2:
```bash
# Бэкапь старый конфиг
cp backend/config.yaml backend/config_v1.yaml

# Обнови всё (см. шаги выше)
```

---

## Улучшения парсера в v2

### Антибан механизмы:
- ✅ Ротация прокси между запросами
- ✅ Случайные задержки 1-3 сек между страницами
- ✅ Реальный User-Agent
- ✅ Правильные HTTP заголовки

### Лучшая фильтрация:
- ✅ Проверка типа автора (private/business)
- ✅ Фильтр по стоп-словам в описании
- ✅ Фильтр по минимальной длине описания (защита от спама)
- ✅ Проверка наличия цены

### Лучшая обработка данных:
- ✅ Множество селекторов для цены (для разных версий вёрстки)
- ✅ Лучшее определение типа автора
- ✅ Обработка ошибок без остановки

---

## Что дальше?

- [ ] Поддержка других платформ (Авито для бизнеса, OLX)
- [ ] Webhook для реальна-тайма
- [ ] PostgreSQL + Redis вместо SQLite
- [ ] Расширенная аналитика
- [ ] Админ-панель для мультиюзер
- [ ] Интеграция с Instagram/TikTok

---

**v2 готова к боевой эксплуатации!** 🚀
