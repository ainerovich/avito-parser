# 🎯 Как использовать Авито Парсер

## Установка и запуск

```bash
# 1. Клонируй репо на VPS
cd /var/www
git clone https://github.com/ainerovich/avito-parser.git
cd avito-parser

# 2. Установи зависимости
cd backend
pip3 install -r requirements.txt
cd ..

# 3. Запусти Dashboard в одном терминале
bash start-dashboard.sh
# Dashboard будет на http://localhost:5000 или http://твой_ip:5000
```

---

## Настройка через Dashboard

### Шаг 1: Выбор города

1. Открой Dashboard: http://localhost:5000
2. Введи город на латинице (как в URL Авито):
   - `vorkuta` → https://www.avito.ru/vorkuta/
   - `moskva` → https://www.avito.ru/moskva/
   - `sankt-peterburg` → https://www.avito.ru/sankt-peterburg/

3. Жми "Сгенерировать ссылки"
4. Система автоматически создаст ссылки для всех категорий

### Шаг 2: Выбор категорий

1. Увидишь список сгенерированных ссылок
2. **Переключателями (toggle)** включи/отключи нужные:
   - 🚗 Автомобили
   - 🏠 Недвижимость (продажа/аренда)
   - ⚽ Спорт и отдых
   - 🏡 Товары для дома
   - 💻 Электроника
   - 🔧 Оборудование

3. Жми "Сохранить ссылки"

### Шаг 3: VK Токен и Группы

1. **Получи VK токен:**
   - Иди: https://vkhost.github.io/
   - Выбери "Standalone приложение"
   - Права: `wall`, `photos`, `groups`, `offline`
   - Скопируй токен

2. **В Dashboard вставь токен** в поле "VK Access Token"

3. **Добавь ID групп для категорий:**
   - Открой группу в VK: `vk.com/club123456789`
   - ID = `-123456789` (с минусом!)
   - Примеры:
     - **🚗 Авторынок:** `-123456789`
     - **🏠 Недвижимость:** `-987654321`
     - **📢 Общая группа:** `-111222333`

4. Жми "Сохранить VK"

### Шаг 4: Telegram (опционально)

1. **Создай бота:**
   - Напиши @BotFather в Telegram
   - Создай нового бота
   - Получи токен формата: `123456789:ABCdef...`

2. **В Dashboard вставь токен** в поле "Telegram Bot Token"

3. **Добавь каналы:**
   - ID канала: `@avto_vorkuta` или числовой ID
   - Добавь бота в админы каналов

4. Жми "Сохранить Telegram"

### Шаг 5: Прокси (опционально)

1. Если нужны прокси (для защиты от блокировки):
   ```
   http://ip:port
   http://user:pass@ip:port
   socks5://ip:port
   ```

2. Вставь по одному на строку

3. Жми "Сохранить прокси"

### Шаг 6: Стоп-слова

1. Добавь слова для фильтрации рекламы:
   - `автосалон`
   - `кредит`
   - `рассрочка`
   - `франшиза`

2. По одному на строку

3. Жми "Сохранить стоп-слова"

---

## Запуск парсера

### В новом терминале:

```bash
cd /var/www/avito-parser
bash start-parser.sh
```

Парсер будет:
- ✅ Запускаться каждые 5 минут (настраивается в config.yaml)
- ✅ Парсить активные ссылки
- ✅ Фильтровать рекламу по стоп-словам
- ✅ Публиковать в VK и Telegram
- ✅ Избегать дублей
- ✅ Логировать всё в `logs/parser.log`

---

## Быстрое наполнение групп

### Для первоначального наполнения:

1. В Dashboard жми одну из кнопок:
   - **"За 1 день"** (~3 страницы)
   - **"За 3 дня"** (~9 страниц)
   - **"За 5 дней"** (~15 страниц)

2. Система запустит парсинг в фоне

3. Все найденные объявления опубликуются в VK и Telegram

4. Следи за статистикой в Dashboard (обновляется каждые 10 сек)

---

## Формат публикуемых объявлений

### Как выглядит пост:

```
📢 iPhone 13 Pro Max

💰 Цена: 45 000 ₽

Отличное состояние, оригинальная коробка в комплекте.
Использовался бережно. Все работает идеально.

🔗 Смотреть объявление на Авито

🏷️ Электроника Воркута | Свежие объявления
```

### Из чего состоит:
1. **Название объявления** - жирный текст
2. **Цена** - выделена крупно с 💰
3. **Описание** - текст из Авито
4. **Ссылка** - кликабельная ссылка на объявление
5. **Подпись** - кастомный текст для категории (настраивается в Dashboard)

---

## Просмотр логов

### Логи парсера:

```bash
# Live просмотр
tail -f /var/www/avito-parser/logs/parser.log

# Последние 100 строк
tail -100 /var/www/avito-parser/logs/parser.log

# Ошибки
grep ERROR /var/www/avito-parser/logs/parser.log
```

### Логи в Dashboard:

1. Открой Dashboard: http://localhost:5000
2. Скролли вниз до секции "Статистика"
3. Видишь обновления в реальном времени

---

## Остановка парсера

```bash
# Найди процесс
ps aux | grep main.py

# Убей процесс
kill PID

# Или если через Docker
docker-compose down
```

---

## Проблемы и решения

### Проблема: VK API ошибка "Invalid access token"
- ✅ Проверь токен в Dashboard
- ✅ Убедись что права `wall, photos, groups, offline`
- ✅ Может нужен новый токен

### Проблема: Не парсятся объявления
- ✅ Проверь логи: `tail -f logs/parser.log`
- ✅ Авито часто меняет вёрстку
- ✅ Может нужны прокси

### Проблема: Не публикуется в VK
- ✅ Проверь ID группы (должен быть с минусом!)
- ✅ Убедись что ты админ группы
- ✅ Проверь что группа не закрыта

### Проблема: TG бот не отправляет сообщения
- ✅ Проверь что бот добавлен в админы канала
- ✅ Проверь ID канала (@username или число)
- ✅ Может нужно написать /start боту в личку

---

## Автоматический запуск при перезагрузке VPS

### Вариант 1: Systemd (рекомендуется)

Создай `/etc/systemd/system/avito-parser.service`:

```ini
[Unit]
Description=Avito Parser
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/var/www/avito-parser
ExecStart=/bin/bash /var/www/avito-parser/start-parser.sh
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запусти:
```bash
systemctl enable avito-parser
systemctl start avito-parser
systemctl status avito-parser
```

### Вариант 2: Docker Compose

```bash
cd /var/www/avito-parser
docker-compose up -d
```

---

## Что дальше?

- [ ] Поддержка нескольких городов одновременно
- [ ] Webhook для публикации в реальном времени
- [ ] Расширенная аналитика
- [ ] Интеграция с Instagram и TikTok
- [ ] Админ-панель для управления несколькими пользователями

---

**Готово! Теперь парсер работает 24/7 и публикует объявления автоматически!** 🚀
